# Seu pedido #<%= @order.id %> foi cancelado

[Seu pedido](<%= public_link_orders_url(token: @order.token) %>) foi **cancelado**, provavelmente porque você pediu.
Caso tenha alguma dúvida, entre em contato com o DA.

Atenciosamente,
DACompSI
